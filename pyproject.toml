[project]
name = "connekt-outreach"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "LeibGit", email = "leibnroth@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "anthropic>=1.1.0",
    "fastapi[standard]>=0.141.1",
    "ruff>=0.16.4",
    "sqlmodel>=0.0.39",
]

[project.scripts]
connekt-outreach = "connekt_outreach:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
